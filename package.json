{
  "name": "react-express-heroku",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "nodemon ./server.js --exec babel-node -e js",
    "build": "babel src -d dist/src && babel ./server.js -d dist",
    "server": "npm run build && NODE_ENV=production node dist/server.js",
    "heroku-postbuild": "cd client && npm install --only=dev && npm install && rimraf build && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-es2015": "^6.24.0",
    "babel-preset-stage-0": "^6.22.0",
    "body-parser": "^1.17.2",
    "cors": "^2.8.3",
    "express": "^4.15.3",
    "graphql": "^0.10.3",
    "graphql-server-express": "^0.9.0",
    "graphql-tools": "^1.1.0",
    "rimraf": "^2.6.1"
  },
  "devDependencies": {
    "nodemon": "^1.11.0"
  }
}
